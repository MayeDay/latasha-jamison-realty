version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  runtime-version: 20
  command: npx serve -s dist -l $PORT
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
